fn __tmpl_render() {
for r: int in (nums) {
__tmpl_emit_lit(0);
__tmpl_emit(str(r));
__tmpl_emit_lit(1);
for c: int in (seq()) {
__tmpl_emit_lit(2);
__tmpl_emit(str(r));
__tmpl_emit_lit(3);
__tmpl_emit(str(c));
__tmpl_emit_lit(4);
}
}
}
