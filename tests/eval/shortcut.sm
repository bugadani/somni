//@ main() == 2

var do_not_touch: int = 0;
fn main() -> int {
    true || increment();
    false || increment();
    false && increment();
    true && increment();

    print(do_not_touch);
    return do_not_touch;
}

fn increment() {
    do_not_touch = do_not_touch + 1;
}
