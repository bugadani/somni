var do_not_touch: int = 0;
fn main() -> bool {
    true || increment();
    false || increment();
    false && increment();
    true && increment();

    print(do_not_touch);
    return assert(do_not_touch == 2);
}

fn increment() {
    do_not_touch = do_not_touch + 1;
}
